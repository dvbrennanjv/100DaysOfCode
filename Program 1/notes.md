1. Printing to python console with the print function
    - print("Hello World")
2. String Manipulation
    - can use \n within a string to create new lines
    - can combine strings by using a +
3. Can take user input with the input function
    - input("prompt")
4. Python Variables
    - can be used to store data
    - len() function can be used to get the length of a string
    - variable types include strings, bools, floats, integers and more
    - variable names should always follow a strict standard
5. Program 1 - Band Name Generator
    - Print out a greeting
    - Ask for user to give the city they grew up in and store in a variable
    - Ask the user for the name of a pet and store it in a variable
    - Combine the name of their city and pet and show them their band name